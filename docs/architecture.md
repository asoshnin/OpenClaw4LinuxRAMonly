# OpenClaw for Linux — System Architecture

> [!NOTE]
> The full technical architecture reference (36 KB, post-Sprint 7) is maintained locally at:
> `_Development/OpenClaw/2026-03-27__12-50_current_state.md`
>
> This file is intentionally **not tracked in git** — it is internal Navigator documentation. If you have cloned the repo, the summary below covers all public-facing architecture details.

---

## Overview

OpenClaw is a **local-first, synchronous, CPU-bound agentic runtime** for Linux x86_64. It is not a web service or a microservice framework — it is a hardened Python runtime designed to run AI agents on your own hardware with full Navigator control over every consequential action.

## Three-Layer Memory Architecture

```
Navigator (You)
    └── Mega-Orchestrator [kimi-orch-01]  ←  architect_tools.py
            └── The Librarian [lib-keeper-01]  ←  librarian_ctl.py
                    └── Vector Archive + Safety Engine  ←  sqlite-vec + Ollama
```

| Layer | Component | Implementation |
|---|---|---|
| **The Map** | `REGISTRY.md` + agent persona `.md` files | Human-readable; auto-generated by `refresh-registry` |
| **The State** | `factory.db` (SQLite WAL) | Agents, pipelines, audit logs, KB proposals |
| **The Archive** | `vec_passages` (sqlite-vec, 768-dim) | Distilled memory; Faint Path semantic search |

## Core Modules

| Module | Location | Role |
|---|---|---|
| `librarian_ctl.py` | `openclaw_skills/librarian/` | DB init, bootstrap, registry, vault ingestion, agent registration |
| `architect_tools.py` | `openclaw_skills/architect/` | Agent runner, HITL deployment gate, vault writer |
| `safety_engine.py` | `openclaw_skills/librarian/` | Epistemic Scrubber — distil + embed + archive |
| `router.py` | `openclaw_skills/` | Dynamic LLM routing (local Ollama ↔ cloud Gemini) |
| `kb.py` | `openclaw_skills/` | Static Knowledge Base injection + KB proposal lifecycle |
| `obsidian_bridge.py` | `openclaw_skills/` | Obsidian Local REST API client (loopback-only) |
| `vector_archive.py` | `openclaw_skills/librarian/` | sqlite-vec table init + `find_faint_paths()` KNN search |
| `self_healing.py` | `openclaw_skills/librarian/` | Circuit-breaking JSON parser (3 retries → RuntimeError) |
| `config.py` | `openclaw_skills/` | `WORKSPACE_ROOT` + explicit Tiered Inference router configuration |

## Security Invariants

1. **HITL Gate** — `deploy_pipeline_with_ui()` requires a native OS dialog click; cannot be bypassed in code.
2. **Burn-on-Read Token** — token file deleted before comparison; no replay window.
3. **Airlock** — `validate_path()` uses `os.path.realpath()` + `os.sep` suffix guard; `PermissionError` on breach.
4. **Routing Halt** — `is_sensitive=True` + `tier=cloud` raises `PermissionError` at the router level; cloud is never called.
5. **Epistemic Scrubber** — all external data (vault notes, web clips) passes through IPI-stripping before vector storage.
6. **Context Guard** — 12,000-character truncation on all LLM inputs (head+tail preserved).
7. **System Agent Protection** — `is_system=1` agents cannot be torn down.

## Test Coverage

**94 tests** across 8 test modules. All HTTP calls to Ollama, Gemini, and Obsidian are mocked — the full suite runs without any live services.

```bash
pytest tests/ -v   # Run all 94 tests
```

## Environment Variables

| Variable | Required | Default | Description |
|---|---|---|---|
| `OPENCLAW_WORKSPACE` | No | `~/.openclaw/workspace` | Workspace root (factory.db, REGISTRY.md, profiles) |
| `GEMINI_API_KEY` | For cloud paths | — | Gemini API key (non-sensitive distillation only) |
| `OLLAMA_REMOTE_URL` | No | `http://192.168.1.8:11434` | Primary inference GPU server endpoint |
| `OLLAMA_LOCAL_URL` | No | `http://127.0.0.1:11434` | Secondary/fallback local inference endpoint |
| `REMOTE_LOCAL_MODEL` | No | `qwen3.5:9b` | Model to utilize on the remote GPU server |
| `OBSIDIAN_API_KEY` | For vault sync | — | Local REST API plugin bearer token |
| `OBSIDIAN_BASE_URL` | No | `http://127.0.0.1:27123` | Must resolve to loopback — enforced at construction |
| `OBSIDIAN_VAULT_PATH` | For bootstrap | — | Absolute path to vault root |

See `.env.example` for full documentation of all variables.
