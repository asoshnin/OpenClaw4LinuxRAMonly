# Agentic Factory - Strategic Backlog (2026-03-27)

## 🟢 Active: Sprint 7 — Obsidian Bidirectional Sync
**Spec:** `_Development/OpenClaw/Sprint_7_ObsidianSync/`

### Block A — Linux Installation & Vault Bootstrap
- [ ] `docs/linux_obsidian_setup.md` — AppImage + plugin install runbook
- [ ] `obsidian_vault_bootstrap.py` — idempotent `os.makedirs` for Johnny.Decimal folders
- [ ] `setup.sh` updates: vault bootstrap + health check (non-blocking)

### Block B — `ObsidianBridge` Module
- [ ] `openclaw_skills/obsidian_bridge.py` — full HTTP client class
- [ ] `ping()`, `read_note()`, `write_note()`, `append_to_note()`, `list_notes()`, `check_obsidian_health()`
- [ ] All calls: `urllib.request`, 10s timeout, bearer token, URL-encoded paths

### Block C — Agent Output → Vault Note
- [ ] `write_agent_result_to_vault()` in `architect_tools.py`
- [ ] Auto-path: `00 - INBOX/openclaw/YYYY-MM-DD_{agent_id}_{slug}.md`
- [ ] Non-fatal on Obsidian down; logs `VAULT_WRITE_SKIPPED`

### Block D — Vault Note → Archive
- [ ] `ingest_vault_note()` + `ingest-vault-note` CLI in `librarian_ctl.py`
- [ ] `source_type='external'` — always scrubbed
- [ ] Raises `RuntimeError` on Obsidian down (explicit failure)

### Block E — Tests (target: ≥ 75)
- [ ] `tests/test_obsidian_bridge.py` — 15+ mocked tests
- [ ] Write/ingest integration tests in existing test files

### Block F — Docs
- [ ] Linux runbook, current_state.md update, README update

---

## ✅ Completed

### Block A — Dynamic LLM Router
- [ ] `router.py` with 6-row routing decision matrix
- [ ] Ollama availability ping before every local call
- [ ] `ROUTING_HALT` sentinel: log + raise on `is_sensitive=True + tier=cloud`
- [ ] All routing outcomes logged to `audit_logs`

### Block B — Static Knowledge Base & Reflection Queue
- [ ] `knowledge_base.json` with `security_rules`, `capability_boundaries`, `epistemic_invariants`
- [ ] `kb.py`: `load_knowledge_base()`, `format_kb_for_prompt()`, `submit_kb_proposal()`, `approve_kb_proposal()`
- [ ] Migration: `proposed_kb_updates` table
- [ ] `run_agent()` updated with KB prefix injection in prompt

### Block C — Self-Healing JSON Parser
- [ ] `self_healing.py`: `parse_json_with_retry()` circuit breaker (max 3 retries → RuntimeError)
- [ ] `_distill_local()` refactored to use circuit breaker (no silent fallback)

### Block D — Scoped Epistemic Scrubber
- [ ] Migration: `source_type TEXT DEFAULT 'external'` on `distilled_memory`
- [ ] `archive_log()` updated with `source_type="internal"|"external"` routing (skip distillation for internal)

### Block E — Acceptance Gate
- [ ] `pytest tests/ -v` → ≥ 58 tests passing
- [ ] Security audit: no hardcoded paths, no silent cloud fallback, no agent self-approval
- [ ] Documentation updated

---

## ✅ Completed

- [x] **Sprint 6 — Core Intelligence & Resilience** *(2026-03-27, 60/60 tests)*
  - `router.py` — Dynamic LLM Router with HITL halt policy (4 routing outcomes, all audited)
  - `knowledge_base.json` + `kb.py` — Static KB injection + HITL-supervised reflection queue
  - `self_healing.py` — Circuit-breaking JSON parser (max 3 retries → RuntimeError)
  - `safety_engine.py` — Scoped scrubber (`source_type` parameter, internal → skip distillation)
  - `migrate_db.py` — `proposed_kb_updates` table + `source_type` column on `distilled_memory`
  - `run_agent()` updated: KB → identity → memory → task prompt order
  - 32 new tests (Sprint 6) + 28 existing = 60 total
- [x] **Sprint 5 — OSS Readiness & Minimal Agent Runner** *(2026-03-27)*
  - `config.py` + `OPENCLAW_WORKSPACE` env var (GAP-02 closed)
  - `setup.sh` single-command cold start
  - `requirements.txt` with pinned versions
  - `run_agent()` + `run` CLI subcommand
  - `tests/` pytest suite (28 tests, 100% pass)
  - `description` + `tool_names` columns on `agents`
  - README repositioned with comparison table
- [x] **Repository Cleansing (Sprint 4)**
- [x] **Meta-Assistant Prompt (Sprint 4)**

---

## Phase 3: Ecosystem & Integration
- [ ] **Obsidian Bidirectional Sync:** Use Obsidian Local REST API exclusively. Guarantees atomic writes.
- [ ] **Health-Check Supervisor (Pre-flight Ping):** Use `urllib` to ping service ports. On failure, `systemctl --user start <service>`. Halt if recovery fails after one retry.
- [ ] **SQLite-backed Async Task Queue:** Enforce WAL mode, separate DB connections for readers (UI) and writers (background workers).
