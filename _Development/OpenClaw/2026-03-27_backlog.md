# Agentic Factory - Strategic Backlog (2026-03-27)

## Phase 1: DX & Metaprogramming (High Priority)
- [x] **Meta-Assistant Prompt:** Create a comprehensive System Prompt for external LLM assistants.
- [ ] **Repository Cleansing:** Migrate the OpenClaw implementation into a clean, dedicated GitHub repository. Create an architecture diagram. Ensure `.gitignore` is strictly configured.
- [ ] **Enhanced Discovery:** Upgrade the `factory.db` schema to include rich `description` and `capabilities` fields for agents/tools. 

## Phase 2: Core Intelligence & Resilience
- [ ] **Dynamic LLM Router (HITL-Guarded):** Implement a routing engine evaluating task complexity vs. data sensitivity.
 - *SECURITY CONSTRAINT:* If `is_sensitive=True` AND `min_model_tier=cloud`, the Router MUST NOT fallback to cloud automatically. It must halt the specific task, set its DB state to `pending_hitl`, and output: `[SYS-HALT: HITL REQUIRED - SENSITIVE CLOUD ROUTING]`. The Navigator must explicitly approve, abort, or sanitize the payload via the UI/CLI.
- [ ] **Static KB Injection & Reflection Queue:** Implement the agent knowledge base architecture.
 - *CONSTRAINT:* Do NOT vectorize core agent rules. Keep `knowledge_base.json` as a static string injected into the system prompt.
 - *FEATURE:* Implement a `proposed_kb_updates` SQLite table. Agents can propose rule additions (via Mode 2 JSON Patch outputs) to this queue for HITL approval and merge.
- [ ] **Self-Healing Parsers:** Implement auto-retry logic for JSON validation with a strict Circuit Breaker (max 3 retries) using the local LLM. Fallback to HITL if the limit is reached.

## Phase 3: Ecosystem & Integration
- [ ] **Obsidian Bidirectional Sync:** Implement tools to read/write to the local Obsidian vault.
 - *SECURITY CONSTRAINT:* Exclusively use the Obsidian Local REST API to guarantee atomic writes.
- [ ] **Health-Check Supervisor (Pre-flight Ping):** Implement a lightweight startup check in OpenClaw.
 - *SECURITY CONSTRAINT:* Use `aiohttp` to ping service ports. If down, execute `systemctl --user start <service>` instead of raw subprocesses. Halt system if recovery fails after one retry.
- [ ] **Asynchronous Task Queue:** Implement a background queue mechanism using SQLite.
 - *SECURITY CONSTRAINT:* Explicitly enforce `PRAGMA journal_mode=WAL;` and separate DB connections for readers (UI) and writers (Background Workers) to prevent lockouts.
